import React from "react";
import { Form, Input, Radio, Tag, Tooltip, List, Table } from "antd";
import { PlusOutlined } from "@ant-design/icons";

export default () => {
    return (
        <Table />
    )
}